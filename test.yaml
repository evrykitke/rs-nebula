# Test configuration (NEBULA_ENV=test). Database URL comes from
# test.local.yaml (gitignored) or NEBULA__DATABASE__URL.

server:
  host: 127.0.0.1
  port: 5001
  request_timeout_secs: 10

logging:
  level: warn
  format: pretty

currencies:
  - { code: KES, minor_units: 2 }
  - { code: USD, minor_units: 2 }
  - { code: JPY, minor_units: 0 }
